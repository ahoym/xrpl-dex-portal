/** Standard text input styling with focus ring. */
export const inputClass =
  "mt-1 w-full border border-zinc-300 bg-white px-3 py-2 text-sm shadow-sm placeholder:text-zinc-400 dark:border-zinc-700 dark:bg-zinc-800/80 dark:placeholder:text-zinc-500";

/** Form field label. */
export const labelClass =
  "block text-xs font-semibold uppercase tracking-wide text-zinc-500 dark:text-zinc-400";

/** Blue primary action button with shadow and hover lift. */
export const primaryButtonClass =
  "bg-blue-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-blue-500 hover:shadow-md active:scale-[0.98] disabled:opacity-50 disabled:hover:shadow-sm disabled:active:scale-100";

/** Secondary (outline) button. */
export const secondaryButtonClass =
  "border border-zinc-300 bg-white px-4 py-2 text-sm font-medium text-zinc-700 shadow-sm hover:bg-zinc-50 hover:border-zinc-400 active:scale-[0.98] dark:border-zinc-700 dark:bg-zinc-800/80 dark:text-zinc-300 dark:hover:bg-zinc-800 dark:hover:border-zinc-600";

/** Danger (outline) button. */
export const dangerButtonClass =
  "border border-red-200 px-4 py-2 text-sm font-medium text-red-600 shadow-sm hover:bg-red-50 hover:border-red-300 active:scale-[0.98] dark:border-red-800 dark:text-red-400 dark:hover:bg-red-950 dark:hover:border-red-700";

/** Card wrapper with shadow. */
export const cardClass =
  "border border-zinc-200/80 bg-white p-5 shadow-sm dark:border-zinc-800 dark:bg-zinc-900/80";

/** Card section header. */
export const cardHeaderClass =
  "text-base font-semibold text-zinc-900 dark:text-zinc-100";

/** Red error text for form errors. */
export const errorTextClass =
  "text-sm font-medium text-red-600 dark:text-red-400";

/** Green success banner. */
export const successBannerClass =
  "mt-3 bg-green-50 p-3 text-center text-sm font-semibold text-green-700 dark:bg-green-900/30 dark:text-green-400";

/** Duration (ms) to show success messages before auto-clearing. */
export const SUCCESS_MESSAGE_DURATION_MS = 2000;
